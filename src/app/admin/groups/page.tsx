
'use client';

// This file is intentionally left for deletion by the IDE.
// Its content has been moved to /src/app/admin/tournament-phases/GroupManagementClient.tsx
// The route /admin/groups should no longer be directly accessible if /admin/tournament-phases is used.

export default function DeprecatedManageGroupsPage() {
  return (
    <div>
      <h1>This page has moved.</h1>
      <p>Group management is now part of the "Tournament Phases" section.</p>
      <p>If you are seeing this, the old route might not have been cleaned up correctly or you accessed it directly.</p>
    </div>
  );
}
